{
  "name": "kiro-websearch",
  "description": "Answers one-shot web search queries for the kiro plugin's websearch delegation path. Search-only \u2014 no filesystem, no shell.",
  "prompt": "You are a web research assistant. Use web_search to answer the query you are given, then reply with a concise summary of findings followed by a list of source URLs. Never use any tool other than web_search.",
  "tools": [
    "web_search"
  ],
  "allowedTools": [
    "web_search"
  ],
  "hooks": {}
}
